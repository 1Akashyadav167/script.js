const power=300;
if(power>100){
    var fly= true;
    console.log(`you can fly is true or false:${fly}`);

}
console.log(`you can fly is true or false:${fly}`);

/// if there is const or let keyword use the 7 line not execute as it shown the fly is not declare....as var has functional scope